## 任务中心 Phase 3 全部完成 + 标签管理

### 关联 Issue

- #24: [P3-T1] 看板拖拽支持
- #25: [P3-T2] 任务模型重构与类型扩展
- #26: [P3-T3] 任务树视图
- #27: [P3-T4] 任务-标准联动
- #28: [P3-T5] 任务关系与依赖
- #29: [P3-T6] 状态机守卫真实化
- #30: [P3-T7] 整改与派生任务

### 额外完成

- GitHub Issue 看板清理：关闭 Phase 1/1.5 共 9 个已完成 Issue
- `nav.utils.ts` → `navigation.ts` 迁移，统一 hash 写入
- 任务标签管理（增删交互、持久化）
- 安装 `@mui/x-tree-view@9.0.4`

### 修改内容

- **路由**: `src/config/navigation.ts` 合并 nav.utils 的 `navigateByHash`/`isHashRouteActive`
- **看板**: `TaskKanbanView.tsx` 重构为 SortableCard + SortableContext + 守卫校验 + 拖拽验证
- **树视图**: `TaskTreeView.tsx` 重写为 MUI X TreeView (SimpleTreeView + TreeItem) WBS 表格风格
- **任务详情**: `TaskDetailPage.tsx` 增加关系管理 UI、标准绑定 Dialog、整改派生链路、标签编辑器
- **任务管理**: `TaskManagementPage.tsx` 守卫接入 transitionTaskStatus、标准绑定回调、关系 CRUD、标签持久化、整改统计
- **Repository**: `taskRepository.ts` 新增关系 CRUD、标准绑定方法、整改创建修复
- **Mock 数据**: `taskManagement.data.ts` 新增标准文档映射、mock 依赖关系、整改任务、标准快照加载
- **守卫**: `taskStateMachine.guards.ts` getPredecessorTasks 支持 TaskRelation 查询
- **类型**: `taskManagement.types.ts` TaskStats 新增 rectification 字段

### 验证

- lint: 零新增 errors（6 个预存 errors unchanged）
- build: TypeScript 零新增 errors
- test: 516 passed, 54 suites

### 技术债务

- 6 个预存 TS 错误待修复（@testing-library/react 依赖、ExportPanel props、DataTable 类型、taskRepository 类型）
- `owner` → `assigneeName` 迁移待完成
- `brandId`/`storeId`/`workPackageId`/`plannedWorkHours`/`actualWorkHours`/`closeReason`/`updatedBy` 未使用字段待清理
