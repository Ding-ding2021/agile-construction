## 页面布局规范建立 + 任务管理模块试点（计划阶段） 🔧

- **时间**: 07:38:07

## 计划概览

### 背景

- TaskManagementPage 有 132 行 `tm-*` 自定义 CSS，与 AppShell/PageHeader/StatsCards 共享组件功能重复
- PmButton/PmInput/PmTable MUI 封装已创建但未被使用
- AppShell 组件存在但未被任何页面使用
- layout-preview.html 在 worktree 中待正式化

### 计划产出物

1. `docs/00-governance/page-layout-standard.md` — 新布局规范（SSOT），含 AppShell 5 层结构、区域命名表、MUI 组件规范
2. MUI wrapper 正式导出（shared/index.ts）
3. TaskManagementPage 重构：AppShell 布局 + PmButton/PmInput 替换
4. index.css `tm-*` 布局 CSS 清理

### 红线

- 不修改 theme.ts、domain/、data/
- 不修改 TaskListView/TaskDetailPage
- 试点范围仅限页面框架层

### 状态

⏳ 规范待评审，暂不执行

---

## 清理失效文件：移除 @mui/lab + 归档 16 个死文件 🔧

- **时间**: 07:55:45

## 问题/需求

项目中有大量无人引用的"死文件"需要清理：

1. `@mui/lab` 依赖零引用
2. 16 个组件/数据文件没有任何外部导入/引用
3. 项目编译和 lint 无新增错误

## 修改内容

### 依赖清理

- **package.json**: 删除 `@mui/lab` 依赖行
- **package-lock.json**: npm install 后自动清理

### 死文件归档（16 个文件 → `.archive/`）

```
.archive/components/personnel/
├── ProjectDetailDrawer.tsx    (299行)
└── UserTable.tsx              (106行)

.archive/components/project/
├── ProjectDigitalEmployeeView.tsx     (130行)
├── ProjectMembersCard.tsx             (222行)
├── ProjectMembersView.tsx             (677行)
├── ProjectRisksCard.tsx               (190行)
├── ProjectTaskAndWbsView.tsx          (77行)
├── ProjectTeamView.tsx                (90行)
├── ProjectTemplateView.tsx            (124行)
├── TaskDispatchPanel.tsx              (102行)
├── projectDigitalEmployee.data.ts     (534行)
├── projectTeam.data.ts                (424行)

.archive/components/project/tabs/
├── ProjectOverviewTab.tsx    (20行, 占位)
└── ProjectScheduleTab.tsx    (20行, 占位)

.archive/components/shared/hooks/
└── useListState.ts           (48行)

.archive/components/standard/
└── template-instantiation.ts (203行)
```

### 验证结果

- ✅ `tsc --noEmit` 通过（0 错误）
- ✅ `tsc -b` 通过
- ✅ `vite build` 成功（862ms）
- ✅ `eslint src/` 0 errors, 37 warnings（均为已有 no-explicit-any 警告）

## 验证

- lint: 0 errors, 37 warnings (已有, 无新增)
- build: 通过
- type-check: 通过

---

## 清理计划文件夹和截图/设计资源文件 🔧

- **时间**: 08:01:05

## 问题/需求

清理项目中的开发过程遗留文件：历史计划文档、截图、设计稿资源等。

## 清理内容

### 1. `.sisyphus/` 整个目录 — 43 个文件 (344KB)

- plans/ (7个), evidence/ (24个), notepads/ (2个), drafts/ (2个), boulder.json

### 2. 根目录 21 个截图文件 (~9.4MB)

- `screenshot_*.png`, `page-*.png`, `task-*.png`, `engineer-assistant-page.png`

### 3. `.codebuddy/figma/` — 34 个 Figma 设计稿导出 (17MB)

- 17 个设计版本的 HTML + screenShot.png

### 4. `.codebuddy/` 其他历史文件

- auto-commit-log.md, daily-logs/, automations/, rules/, scripts/

### 5. `public/assets/CodeBubbyAssets/` 全部清空

- 与 `assets/CodeBubbyAssets/` 重复的目录(283_2, 374_2, 3848_19, 3990_3 等)已删除
- 仅含 figma.html 占位的空目录已删除

### 6. `assets/CodeBuddyAssets/` 整个目录 — 515 个文件 (2MB)

- 8 个子目录 (4287_2, 4203_756, 4106_5251 等)，全部未在源码中引用

### 7. `src/assets/hero.png, react.svg, vite.svg` — 3 个 Vite 模板文件

### 保留的资产

- `assets/CodeBubbyAssets/` (完整保留，被 icons/index.ts、StatCard、各页面引用)

## 验证

- ✅ `npm run build` — 726ms 构建成功
- ✅ `eslint src/` — 0 errors, 37 warnings (已有)
- ✅ `tsc -b` — 通过

## 变更统计

- 976 个文件删除, 16 个重命名(死文件归档), 3 个新增, 4 个修改
- 总计 1005 个文件变更

---
