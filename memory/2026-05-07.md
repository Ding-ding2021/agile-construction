## src-next 测试基础设施搭建 + 测试用例扩充 ✨

- **时间**: 13:13

### 关联 Issue

- PLAN.md 待办：src-next 测试基础设施搭建

### 合并冲突修复

- 修复 root `package.json` 的 stashed merge conflict（清理了重复 workspace 依赖）
- 修复 `docs/03-engineering/development-plan-v1.2.md` 的 merge conflict

### 依赖补齐

- 将 `tw-animate-css`、`tailwind-merge`、`@testing-library/user-event` 加入 `src-next/package.json`（原被根 package.json 隐式提供，现显式声明）

### 测试基础设施搭建（PLAN.md 待办 ✅）

- `src-next/vitest.config.ts`：添加覆盖率阈值（lines/statements 10%, functions 8%, branches 5%）
- 已有 `src/test/setup.ts`（`@testing-library/jest-dom`）正常运作

### 新增测试文件（6 个）

| 文件                          | 用例数 | 测试内容                                              |
| ----------------------------- | ------ | ----------------------------------------------------- |
| `badge.test.tsx`              | 3      | 渲染文本、variant、className 透传                     |
| `card.test.tsx`               | 2      | 基础渲染、compound Card 组合                          |
| `input.test.tsx`              | 3      | placeholder、输入交互、className 透传                 |
| `cn.test.ts`                  | 4      | 合并、条件、Tailwind 冲突合并、空输入                 |
| `section-cards.test.tsx`      | 5      | 多指标渲染、trend/description、className 透传、空数据 |
| `personnel-constants.test.ts` | 6      | 4 标签常量完整性、style 结构、avatarColor 确定性      |

### 修改内容

- `package.json` — 修复 merge conflict
- `src-next/package.json` — 补齐 3 个显式依赖
- `src-next/vitest.config.ts` — 添加 coverage thresholds
- `src-next/src/__tests__/badge.test.tsx` — 新增
- `src-next/src/__tests__/card.test.tsx` — 新增
- `src-next/src/__tests__/input.test.tsx` — 新增
- `src-next/src/__tests__/cn.test.ts` — 新增
- `src-next/src/__tests__/section-cards.test.tsx` — 新增
- `src-next/src/__tests__/personnel-constants.test.ts` — 新增
- `docs/03-engineering/development-plan-v1.2.md` — 修复 merge conflict
- `docs/PLAN.md` — 更新待办状态
- `AGENTS.md` — 更新测试命令说明

### 验证

- lint: 0 errors, 0 warnings
- test: 8 files, 31 tests, all passed

## 前端功能开发必须使用 E2E 测试 ✨

- **时间**: 00:32:21

新增 E2E 测试强制要求，前端功能必须有 Playwright E2E 覆盖

### 修改文件

- `.agents/skills/squad-pre-dev-evaluation/SKILL.md` — 技术评估员测试策略新增 E2E 项
- `.agents/skills/squad-post-dev-review/SKILL.md` — 功能验收 checklist 新增 E2E 覆盖/通过检查
- `docs/00-governance/agent-squad-protocol.md` — 测试策略模板新增 E2E
- `AGENTS.md` — 新增测试约束章节

---

## 创建 Skills/Agents/MCP 全景扫描工具 ✨

- **时间**: 02:08:25

创建了 `scripts/scan-tools.py`，扫描并统计项目所有技能、Agent 角色、MCP 服务器、工作日志

### 功能

- 扫描项目 skills（`.agents/skills/`，32个）和全局 skills（`.config/opencode/skills/`，21个）
- 扫描 MCP 配置（3个服务器，含状态）
- 扫描 Agent 角色定义
- 扫描工作日志天数
- 输出 CLI 概览或 Markdown 报告

### 命令

- `python scripts/scan-tools.py` — CLI 摘要
- `python scripts/scan-tools.py --md` — Markdown 报告
- `python scripts/scan-tools.py --md --save` — 保存到 docs/SCAN-REPORT.md

### 修改

- 新建 `scripts/scan-tools.py`
- 新建 `docs/SCAN-REPORT.md`（首次生成）
- 更新 `AGENTS.md`（常用命令追加全景扫描）

---

## 搭建 AI 开发质量指标体系 + scan-tools.py --report 仪表盘 ✨

- **时间**: 02:31:48

建立了完整的质量追踪体系，每次任务后记录 KPI 数据，scan-tools.py 自动生成仪表盘

### 新增文件

- `docs/00-governance/quality-metrics.md` — KPI 定义、JSON schema、偏差推断机制、数据生命周期
- `.workbuddy/stats/2026-05-07.json` — 第一条基线记录

### 修改文件

- `scripts/scan-tools.py` — 新增 --report 模式（scan_stats / infer_expected_skills / calc_drift / CLI仪表盘 / Markdown仪表盘）
- `AGENTS.md` — 任务结束协议追加 C 节（质量评价）、常用命令追加仪表盘

### 核心指标

- human_interventions（人工干预次数）
- requirement_deviation（需求偏差度）
- rework_rounds（返工轮次）
- bugs_found_post_delivery（交付后 Bug）
- skills_called（Skill 调用列表）

### 偏差机制

从历史记录自动推断同类任务应调 skills（调用率阈值 50%），偏差 = 应调未调 + 误调

---

## [P1.5-T8] 升版开发计划 V2.0 📝

- **时间**: 02:50:11

基于 shadcn 新栈创建 V2.0 开发计划文档，替代已废弃的 V1.2（MUI 旧栈）。Phase 1/1.5 折叠为已完成资产，Phase 2 重新排序为 WBS 阶段2 → 8标签填充 → 标准管理 → 任务数据硬化。Phase 3+ 保留框架。覆盖率策略从全局 20% 改为模块级验收条件。

修改内容：

- 新建 docs/03-engineering/development-plan-v2.0.md
- 更新 docs/PLAN.md（引用、状态、决策记录）
- 关闭 Issue #47

---

## 修复 build 类型错误 🐛

- **时间**: 02:59:23

修复 src-next/ 中 16 个 TypeScript 编译错误：

修复的文件：

- src-next/pages/settings/SettingsPage.tsx — 删除未使用的 imports
- src-next/pages/tasks/components/calendar/CalendarView.tsx — 删除未使用的 ChevronDown
- src-next/pages/tasks/components/kanban/KanbanCard.tsx — 删除未使用的 Progress + 修复 onKeyDown 类型
- src-next/pages/tasks/components/NewTaskDialog.tsx — 修复 Record<string, string | null> 类型
- src-next/pages/tasks/components/operation/TaskAttachments.tsx — 删除未使用的 Paperclip
- src-next/pages/tasks/components/operation/TaskPeople.tsx — 删除未使用的 Button
- src-next/pages/tasks/components/operation/TaskRiskSla.tsx — 修复 Select onChange 类型
- src-next/pages/tasks/components/TaskDetailContent.tsx — 删除未使用的 e
- src-next/pages/tasks/components/TaskDetailHeader.tsx — 删除未使用的 task 参数
- src-next/pages/tasks/components/tree/SubTaskTree.tsx — 删除未使用的 TableHead
- src-next/components/nav-main.tsx — 修复 any 类型 + children 可选
- src-next/components/nav-user.tsx — 删除未使用的 DropdownMenuSeparator
- src-next/components/app-sidebar.tsx — 删除未使用的 Package
- src-next/pages/personnel/PersonnelListPage.tsx — 删除未使用的 DropdownMenuItem
- src-next/vite-env.d.ts — 新增 CSS 模块声明

build 错误从 28 降至 12（剩余为 main 分支预置的 base-ui 类型问题）

---

## P2-T1 Task 1: WBS 工具函数（CPM 算法 + Gantt 工具 + 测试） ✨

- **时间**: 03:15:27

创建了 src-next/lib/wbs-cpm.ts (CPM关键路径算法)，src-next/lib/wbs-gantt-utils.ts (甘特图工具函数)，以及对应的测试文件。在 wbs-utils.ts 中添加了 parseDependencies 函数。全部13个测试通过。

---

## Task 3: Gantt chart 交互 + 视图集成 + 解锁切换 ✨

- **时间**: 03:20:20

完成了甘特图的三个任务：

1. **WBSGanttChart.tsx** — 添加 CPM 关键路径计算、依赖箭头 SVG 覆盖层、工具提示显示浮动时间
2. **WBSView.tsx** — 将甘特图占位符替换为 `<WBSGanttChart />`
3. **WBSToolbar.tsx** — 移除 isDisabled 锁定逻辑，所有标签页可点击切换

构建结果：新代码无错误，仅有其他文件的预先存在错误。

---

## P2-T1 Task 4: WBS 网络图 (React Flow + dagre) ✨

- **时间**: 03:25:05

## 问题/需求

实现 WBS 网络图视图，使用 React Flow + dagre 自动布局 + 自定义节点组件。支持关键路径高亮、节点选中联动。

## 修改内容

- 创建 `src-next/pages/wbs/components/WBSNetworkNode.tsx` — 自定义网络图节点组件（带 Handle、状态着色、关键路径标记）
- 创建 `src-next/pages/wbs/components/WBSNetworkDiagram.tsx` — 主网络图组件（ReactFlow + dagre 分层布局 + CPM 关键路径 + MiniMap/Controls/Background）
- 修改 `src-next/pages/wbs/WBSView.tsx` — 替换网络图占位符为真实组件

## 验证

- lint: 通过
- build: 通过（新增文件零错误，仅预存错误）
- 无新测试（Task 4 为纯 UI 组件集成，E2E 测试在 Task 6 覆盖）

---

## Task 5: Store extension for gantt zoom state ✨

- **时间**: 03:26:03

向 wbsStore.ts 添加 ganttZoom 状态（默认 30px/day）和 setGanttZoom action，支持甘特图缩放状态持久化，与 selectedId 协作实现三视图同步。

---

## P2-T1 WBS 阶段 2 — 甘特图 + 网络图 ✨

- **时间**: 03:28:54

基于 V2.0 开发计划实施 P2-T1。甘特图采用纯 React + SVG 自绘方案（零 MUI 依赖），网络图基于 @xyflow/react + dagre 自动布局。全部 8 个子任务完成，lint 0 errors，13 单元测试通过，build 无新增错误。

新增文件：12 个
修改文件：5 个
新增依赖：@xyflow/react ^12.10.2, dagre ^0.8.5, @types/dagre

---

## Task 2: Gantt timeline zoom level support (day/week/month) ✨

- **时间**: 04:42:31

已将 WBSGanttTimeline.tsx 重构为支持日/周/月三级缩放。导出 ZOOM_LEVELS 常量（day:30, week:7, month:1.5）和 ZoomLevel 类型。组件现在接收 zoom 和 dayWidth props 以动态计算列宽。现有消费者 WBSGanttChart.tsx 需要后续更新以传入新的 props。

---

## Task 3: 修复 WBS 甘特图条组件 — 移除 opacity、纯色语义、无障碍支持 🔧

- **时间**: 04:44:08

替换 src-next/pages/wbs/components/WBSGanttBar.tsx：

- 移除基于 opacity 的色差（isCritical ? 1 : 0.7），改用纯 Tailwind 语义色（STATUS_BAR_COLOR）
- 更换 `px-1.5`（6px）为 `px-2`（8px）— 符合 2px 网格规范
- 添加 keyboard 无障碍（role="button", tabIndex, aria-label, onKeyDown）
- 添加选中态（ring-1 ring-ring）
- 添加父节点（summary bar）支持（isParent prop）
- 添加 blocked 状态红色虚线边框
- 关键路径使用 `border-l-2` 而非 opacity 区分
- 导入 `cn()` 和 `useWBSStore`，移除对 `WBS_STATUS_STYLE` 的依赖

---

## 甘特图增强批次 A — 树形结构+粒度切换+滚动同步 ✨

- **时间**: 05:11:27

批次 A 全部完成。基于 Squad 评估结果实施：

- buildGanttRows 改为递归树形版，支持 expandedIds 展开/折叠过滤
- 新增 summarizeParentDates/flattenVisibleTree 工具函数
- 时间轴支持日/周/月三粒度切换
- 甘特条移除 opacity/透明度违规，改用纯色语义（bg-muted/bg-chart-1/bg-chart-2）
- 甘特图主组件重写：左侧 sticky 信息列 + 右侧甘特条 + 水平同步滚动 + 展开/折叠按钮
- 工具栏增加缩放 ToggleGroup（仅甘特图视图显示）
- 单元测试 20 条全部通过
- E2E 测试 7 条（原有 4 + 新增 3）

---

## 项目日历系统 — 数据模型+API+前端服务+甘特图集成 ✨

- **时间**: 08:48:53

建立可复用的项目日历系统，支持中国建筑行业工作历。

完成内容：

1. Prisma Schema: Calendar + CalendarException 模型
2. 后端 Controller: 7 个 RESTful 端点（CRUD + 批量设例外 + 日期范围查询）
3. 种子日历: 中国建筑通用日历（周一~六工作、周日休息、29 天法定假日）
4. 前端类型 + calendar.ts 纯工具函数（isWorkingDay/getWorkingDays/getNonWorkingDays）
5. calendar-api.ts API 依赖函数（loadNonWorkingDays 带缓存）
6. 甘特图集成: 替换硬编码 weekendDays → API 驱动的 nonWorkingCols
7. 时间轴周末着色: 从硬编码改为日历驱动的非工作日集合
8. 降级策略: API 不可用时回退到纯周末规则
9. 单元测试: 8 条（isWorkingDay 6 + getWorkingDays 1 + getNonWorkingDays 2）

---

## WBS 甘特图 SVAR 替换 + 功能对齐 ✨

- **时间**: 22:21:17

甘特图从自建替换为 SVAR Gantt (MIT)。删除自建代码517行，SVAR 组件132行。

已完成：

- SVAR 甘特图组件（树形层级、依赖箭头、拖拽编辑、进度条）
- 自定义 Toolbar（日/周/月/年预设 + 今日按钮）
- 周日灰色背景（scales[].css）
- 无限日历加载（SVAR 内置，已解决之前卡住的 bug）
- WBS Store 双向同步（拖拽→updateNode→API 持久化）
- CPM 关键路径数据注入 tasks（data 层面）
- 依赖线 SVG polyline（links prop 硬编码验证通过，动态计算待确认）

已知问题：

1. 里程碑 ◆ 未实现（需 taskTemplate）
2. 依赖类型仅 FS（SVAR 内部支持 4 种但 UI 未开发）
3. 缺少 Ctrl+Z 撤销（需 PRO 或 Zustand temporal）

测试：28/28 通过

---
